# Cypress E2E Tests

## Setup

1. Install Cypress:
   npm install --save-dev cypress

2. Run tests:
   npx cypress open

## Test Structure
- `integration/`: Main test specs
- `fixtures/`: Test data
- `support/`: Custom commands
