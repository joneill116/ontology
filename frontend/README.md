# Ontology Editor Frontend

This folder will contain the web-based user interface for the ontology editor.

Recommended structure:
- `src/` – source code (React, Vue, or plain HTML/JS)
- `public/` – static assets
- `README.md` – frontend documentation

You can use React for a modern, clean UI, or plain HTML/JS for simplicity.
